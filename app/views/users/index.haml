= link_to 'New User', new_user_path
-@users.each do |user|
  %p= "Name: #{user.name}"
  %p= "Email: #{user.email}"
  = link_to 'edit', editp_user_path(user)
  = link_to 'delete', user_path(user), method: :delete
